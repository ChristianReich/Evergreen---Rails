<%= form_with(model: prayer) do |form| %>
  <% if prayer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(prayer.errors.count, "error") %> prohibited this prayer from being saved:</h2>

      <ul>
        <% prayer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :prayer_request %>
    <%= form.text_field :prayer_request %>
  </div>

  <div class="field">
    <%= form.label :notes %>
    <%= form.text_area :notes %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date %>
  </div>

  <div class="field form-group">
    <%= form.number_field :user_id , id: :prayer_user_id, class:"form-control", value:current_user.id, type: :hidden %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
